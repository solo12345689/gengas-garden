import React from 'react';
export default function ChannelList({country,channels,play}){ if(!country) return null; return (<div className="absolute top-4 right-4 bg-gray-900/95 w-96 max-h-[80vh] overflow-y-auto rounded shadow-lg z-40 p-3"><div className="flex justify-between items-center mb-2"><div><div className="font-bold text-lg">{country.name}</div><div className="text-sm text-gray-400">{country.capital} • {country.timezone}</div></div><button className="text-red-400" onClick={()=>play(null)}>✕</button></div>{channels?.length===0? <div className="text-gray-400">No channels</div> : channels.map((ch,i)=>(<div key={i} className="p-2 rounded hover:bg-gray-800 cursor-pointer flex items-center gap-2" onClick={()=>play(ch.url)}><div className="w-10 h-6 bg-gray-700 rounded overflow-hidden flex items-center justify-center text-xs">{ch.logo? <img src={ch.logo} alt="" className="w-full h-full object-cover"/>: ch.type==='youtube'? 'YT':'TV'}</div><div className="flex-1"><div className="font-medium text-sm">{ch.name}</div><div className="text-xs text-gray-400">{(ch.tags||[]).join(', ')}</div></div><div className="text-xs text-gray-500">{ch.type}</div></div>))}</div>)}
