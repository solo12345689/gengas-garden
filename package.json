{
  "name": "gengas-garden-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "node server/index.js"
  }
}